import 'package:flutter/material.dart';

class FuelReport extends StatelessWidget {
  const FuelReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Coming Soon")),
    );
  }
}
