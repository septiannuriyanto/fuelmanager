import 'package:flutter/material.dart';

class FTReadiness extends StatelessWidget {
  const FTReadiness({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Coming Soon")),
    );
  }
}
